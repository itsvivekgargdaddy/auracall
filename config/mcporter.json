{
  "mcpServers": {
    "vercel-domains": {
      "command": "npx",
      "args": [
        "-y",
        "vercel-domains-mcp"
      ],
      "env": {
        "VERCEL_ACCESS_TOKEN": "${VERCEL_ACCESS_TOKEN}"
      }
    },
    "firecrawl": {
      "description": "Firecrawl hosted MCP for authenticated scraping and extraction.",
      "baseUrl": "https://mcp.firecrawl.dev/v2/mcp",
      "headers": {
        "Authorization": "Bearer ${FIRECRAWL_API_KEY}"
      }
    },
    "chrome-devtools": {
      "description": "Attach chrome-devtools-mcp to the live Aura-Call browser session.",
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--browserUrl",
        "${CHROME_DEVTOOLS_URL}"
      ],
      "lifecycle": "ephemeral"
    },
    "auracall": {
      "command": "auracall-mcp",
      "args": [],
      "root": ".."
    },
    "auracall-local": {
      "description": "Local auracall-mcp built from dist/bin/auracall-mcp.js",
      "command": "node",
      "args": [
        "../dist/bin/auracall-mcp.js"
      ],
      "root": ".."
    }
  },
  "imports": []
}
